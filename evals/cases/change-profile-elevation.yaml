schema_version: 1
id: change-profile-elevation
target_skill: software-idea-to-project
activation: explicit
fixture: basic
sandbox: read-only
turns:
  - "Use $software-idea-to-project in chat-only mode. Start a proportional analysis for a localized account-label correction in an existing system with a current baseline. Do not approve or implement anything."
  - "New evidence: the label is derived from authorization roles, the role mapping changes who can access records, and existing rows require migration. Continue the analysis and explain the workflow consequence."
assertions: [{type: git-clean, id: elevation-remains-read-only, critical: true}]
forbidden_effects: [workspace-write, gate-bypass, implementation-authorization]
semantic_rubric: change-profile-routing
repetitions: 1
tags: [change-profile, critical, elevation, authorization, persistent-state]
