# Repository disagreement evidence

The checkpoint says the partial C path is `partial-c.txt`, while an old note claims that C was
never started. The evaluator must inspect the repository and surface or reconcile the contradiction
before implementation resumes.
