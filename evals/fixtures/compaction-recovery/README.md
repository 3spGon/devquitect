# Compaction recovery fixture

This fixture represents an approved `project-plan-execution` delivery after operational context
loss. Slices A and B are recorded as completed, C has partial files and is the only active slice,
and D is pending. The evidence files include stale conversational guidance and a deliberate
checkpoint/repository disagreement. They are prompts for inspection, not prefabricated successful
results; the evaluator must reconcile them before continuing.
