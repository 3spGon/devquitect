# Delivery authorization conflict fixture

The definition handoff records authorization for `SLICE-001` only. The delivery tracker
lists `SLICE-002` without any later user authorization. Its own list cannot resolve the conflict.
